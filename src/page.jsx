
var React = require('react');

var Page = React.createClass({
    render: function () {
        return (
            <div>
                <canvas id="canvas"></canvas>
            </div>
        );
    }
});

module.exports = Page;

